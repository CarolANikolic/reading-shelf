<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Book Library</title>
</head>
<body>
    <main>
        <section>
            <div>
                <h1>Books to Read Before You Die</h1>
                <p>Life's too short to miss out on great reads.</p>
            </div>
            <form action="/" method="POST">
                <label for="input-book">Add a book to read:</label>
                <input placeholder="e.g. The Bell Jar - Sylvia Plath" id="input-book" name="item" required></input>
            </form>
    
            <span class="errorMessage"><%= errorMessage %></span>

        </section>

        <% if (items.length > 0) { %>
            <ul>
                <% items.forEach(item => { %>
                    <li 
                    contenteditable="true"
                    data-itemid="<%= item.id%>" >
                            <%= item.content %>
                        <img data-itemread="read" src="../public/images/icon-read.svg"/>
                        <img data-itemdelete="delete" src="../public/images/icon-trash.svg"/>
                    </li>
                <% }); %>
            </ul>
        <% } %>
    </main>

    <script type="module" src="./src/functions/main.js"></script>
</body>
</html>