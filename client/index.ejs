<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>To Do App</title>
</head>
<body>
    <main>
        <form action="/" method="POST">
            <input placeholder="I need to..." name="item" required></input>
        </form>

        <% if (items.length > 0) { %>
            <ul>
                <% items.forEach(item => { %>
                    <li contenteditable="true"><input type="checkbox"><%= item.content %></input></li>
                <% }); %>
            </ul>
        <% } %>
    </main>
</body>
</html>